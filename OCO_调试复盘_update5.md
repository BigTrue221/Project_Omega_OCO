
### 第十三阶段：捍卫“流程正义”，防止模型越俎代庖（2026-04-16 更新）

**问题现象**：
在解决长文本超时问题时，最初的方案是截断 `Critic` 的输入文本以防解析报错。但这违背了**“流程正义”**，因为这掩盖了真正的问题：`Planner`（本地模型 Qwen 3.5 27B）并未履行“拆解任务并调度工具”的职责，而是错误地利用 `direct_response` 虚拟工具，强行一口气输出了上千字的小说正文。这不仅会导致飞书手机端阅读体验变差（如果是截断的半成品内容），更重要的是破坏了流水线（Pipeline）设计的核心意义，导致后续针对 AI 视频生成、游戏开发等需要多步骤严格协同的流水线无法正常运作。

**排查与修复**：
1. **撤销截断，保留真实反馈**：回滚 `nodes/critic.py` 中的截断逻辑，确保执行结果完整无损地流转和透传。
2. **重塑 Planner 角色定位（强制流程正义）**：大幅强化了 `nodes/planner.py` 中的 System Prompt。明确定义其身份为**“任务规划与调度器”**，加入【流水线与工具使用规范】：
   - **严禁越俎代庖**：明确禁止 Planner 在不调用专业工具的情况下直接生成长篇内容或交付物。
   - **强制任务拆解**：要求对于复杂任务（如小说创作），必须拆解为多个步骤并调用对应工具（如 `generate_outline`, `write_chapter`）。
   - **严格限制 `direct_response`**：明确警告该虚拟工具仅能用于“简单闲聊”和“能力缺失反馈”，绝对不能用于输出正文或代码。

此架构级调整彻底纠正了 Planner 的认知越界问题，确保了系统按预期的多节点流水线（先大纲、后正文）稳定运行，为未来的复杂工作流奠定了坚实的基础。

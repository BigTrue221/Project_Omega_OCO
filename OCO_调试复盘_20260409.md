# OCO 认知闭环系统调试复盘

> 本文档记录 2026-04-09 对 OCO 认知闭环系统故障的完整排查与修复过程。

---

## 一、故障现象

飞书机器人始终回复：
```
🔌 连接工具 正在连接 MCP 工具服务器...
```
然后卡住，无后续响应。

---

## 二、系统架构

```
飞书 → Cloudflare Tunnel → bot_server (5000) → OCOCognitiveLoopAdapter
                                                    ↓
                                              novel_mcp_server (stdio)
                                                    ↓
                                              LangGraph API (5005)
```

**启动顺序**：LangGraph API → novel_mcp_server → bot_server → cloudflared

---

## 三、排查过程纪要

### 第一阶段：初步诊断

**尝试**：直接运行测试脚本，观察 bot_server 日志输出。

**发现**：
- LangGraph API (5005) 正常响应
- novel_mcp_server 进程存在但似乎未正常工作
- `session.initialize()` 调用超时

**代码审查**：
- 检查 `bot_server.py` 中的异常处理
- 检查 `client.py` 中的连接逻辑

### 第二阶段：发现 bot_server.py 异常处理问题

**问题**：`bot_server.py` 使用 `except Exception`，这在 Python 3.11+ 中不会捕获 `asyncio.CancelledError`。

```python
# 问题代码
try:
    await asyncio.wait_for(coro, timeout=timeout)
except Exception as e:  # CancelledError 不是 Exception 的子类！
    logger.error(f"连接 MCP 服务器被取消")
```

**修复**：
```python
# 修复后
except BaseException as e:
```

**验证**：问题依然存在，说明这不是唯一问题。

### 第三阶段：发现 client.py 严重缩进错误

**问题**：使用 IDE 检查 `client.py`，发现 `_connect()` 函数存在严重缩进错误。

```python
async def _connect():
    context = stdio_client(server_params)
    # ❌ 缩进错误！下面的代码本应在 _connect() 内部
#     ↓ 但缩进与 _connect 定义平齐，实际在函数外部
    reader, writer = await asyncio.wait_for(context.__aenter__(), timeout=20)
    session = ClientSession(reader, writer)
    await asyncio.wait_for(session.initialize(), timeout=20)  # 这行代码未在协程中执行！
```

**影响**：
- `_connect()` 创建 `context` 后立即返回
- 实际的连接代码（`reader/writer` 获取、`session.initialize()`）在 `add_server()` 主流程中同步执行
- 协程调度混乱，`wait_for` 超时机制失效

**修复**：重写整个连接逻辑，确保所有异步操作在正确的协程上下文中执行。

### 第四阶段：mcp/fastmcp 版本不兼容

**发现**：在检查 mcp 库版本时发现依赖问题。

```
mcp: 1.19.0 (venv)
fastmcp: 3.2.0 (venv)
```

**错误**：
```
ImportError: cannot import name 'ServerTasksCapability' from 'mcp.types'
```

**分析**：`fastmcp 3.2.0` 需要 `ServerTasksCapability`，但 `mcp 1.19.0` 没有这个类型。

**尝试修复**：
1. 升级 mcp 到 1.26.0 → 失败（pydantic 版本冲突）
2. 升级 pydantic 到 2.12.5 → 失败（其他依赖冲突）
3. 降级 fastmcp 到 2.14.6 → 勉强可用

**结论**：版本依赖问题，但即使版本兼容，核心问题仍未解决。

### 第五阶段：深入协议层调试

**发现**：即使 Server 和 Client 都能发送 JSON-RPC 消息，`session.initialize()` 仍然挂起。

**诊断方法**：在 Server 端添加详细日志，确认请求接收和响应发送。

**观察**：
```
收到请求: {"method":"initialize",...}
发送响应: {"jsonrpc":"2.0","id":0,"result":{...}}
```

Server 确实发送了正确的响应，但 Client 仍然挂起。

### 第六阶段：发现 mcp 库 ClientSession 与框架不兼容

**发现**：直接用 Python `subprocess` + `asyncio` 手动实现 Client，完全绕开 `mcp` 库的 `ClientSession`，**竟然成功了！**

```python
# 手动客户端 - 完全正常
async def manual_test():
    proc = await asyncio.create_subprocess_exec(...)
    # 手动发送 initialize 请求
    # 手动读取响应
    # 手动发送 initialized 通知
    # 手动调用 tools/list
    # 全部正常！
```

**结论**：问题在于 `mcp` 库的 `ClientSession` 实现与 Server 之间的交互细节存在不兼容。

**根因分析**：
- `mcp` 库使用 `anyio.streams.memory.MemoryObjectReceiveStream` 作为消息通道
- `ClientSession.initialize()` 内部发送 `InitializedNotification` 后等待服务器确认
- Server 收到通知后没有发送确认响应（因为是 notification，不是 request）
- 但 Client 的 `_response_futures` 字典可能存在问题，导致响应匹配失败

### 第七阶段：完全绕过 mcp 库 ClientSession

**解决方案**：重写 `novel_mcp_server.py` 和 `client.py`

#### 7.1 重写 novel_mcp_server.py

**设计决策**：
- 移除对 `mcp.server.Server` 框架的依赖
- 使用纯 Python `json` 模块手动处理 JSON-RPC
- stdout 只输出 JSON-RPC 响应（不混入日志）
- stderr 输出日志（不被 Client 读取）

```python
class JSONRPCServer:
    def handle_request(self, request: Dict[str, Any]) -> Optional[Dict[str, Any]]:
        method = request.get("method")
        req_id = request.get("id")

        if method == "initialize":
            return {"jsonrpc": "2.0", "id": req_id, "result": {...}}
        elif method == "notifications/initialized":
            return None  # 不返回响应
        elif method == "tools/list":
            return {"jsonrpc": "2.0", "id": req_id, "result": {...}}
        # ...
```

**关键点**：
- `notifications/initialized` 不返回响应（按 JSON-RPC 规范）
- 所有 JSON 输出到 stdout
- 所有日志输出到 stderr

#### 7.2 创建 simple_client.py

**设计**：全新的轻量级 MCP Client，直接使用 asyncio subprocess 通信。

```python
class SimpleMCPClient:
    async def connect(self, command: str, args: List[str], env: Dict[str, Any] = None):
        self.proc = await asyncio.create_subprocess_exec(
            command, *args,
            stdin=asyncio.subprocess.PIPE,
            stdout=asyncio.subprocess.PIPE,
            stderr=asyncio.subprocess.PIPE,
        )
        self._read_task = asyncio.create_task(self._read_stdout())
        await self._wait_for_server_init()
        await self._send_initialize()
        await self._load_tools()
```

**关键机制**：
- `_read_stdout()` 后台任务持续读取 Server 输出
- `_response_futures` 字典通过 request ID 匹配响应
- `call_tool()` 通过 future 异步等待响应

#### 7.3 重写 client.py

**保留** `OCO_MCPClient` 类的接口不变，内部改用 `SimpleMCPClient`。

```python
class OCO_MCPClient:
    async def add_server(self, config: MCPServerConfig, timeout: float = 15.0):
        client = SimpleMCPClient()
        await asyncio.wait_for(
            client.connect(config.command, config.args, config.env),
            timeout=timeout
        )
        self.servers[config.name] = client
        # ...
```

---

## 四、修复文件清单

| 文件 | 修复内容 |
|------|----------|
| `bot_server.py` | `except Exception` → `except BaseException` |
| `client.py` | 完全重写，使用 SimpleMCPClient |
| `simple_client.py` | 新增，轻量级 MCP Client 实现 |
| `novel_mcp_server.py` | 完全重写，使用纯 JSON-RPC |
| `mcp/__init__.py` | 新增，模块导出 |

---

## 五、验证结果

### 5.1 服务状态检查

```bash
$ lsof -i :5000 -i :5005 | grep LISTEN
python  16428  pengren    8u  IPv4  74692  TCP *:5005 (LISTEN)  # LangGraph
python  16471  pengren    3u  IPv4  96663  TCP *:5000 (LISTEN)  # bot_server

$ ps aux | grep novel_mcp_server
pengren    16470  ... novel_mcp_server.py  # 运行中
```

### 5.2 MCP Client 连接测试

```
INFO:OCO_MCPClient:正在连接到 MCP 服务器：novel_mcp...
INFO:MCP_Client:MCP 服务器连接成功: {'name': 'Novel_LangGraph', 'version': '1.0'}
INFO:MCP_Client:加载了 8 个工具
INFO:OCO_MCPClient:发现工具：generate_outline (来自服务器：novel_mcp)
INFO:OCO_MCPClient:发现工具：write_chapter (来自服务器：novel_mcp)
INFO:OCO_MCPClient:发现工具：revise_chapter (来自服务器：novel_mcp)
INFO:OCO_MCPClient:发现工具：review_chapter (来自服务器：novel_mcp)
INFO:OCO_MCPClient:发现工具：get_state (来自服务器：novel_mcp)
INFO:OCO_MCPClient:发现工具：goto_node (来自服务器：novel_mcp)
INFO:OCO_MCPClient:发现工具：list_nodes (来自服务器：novel_mcp)
INFO:OCO_MCPClient:发现工具：confirm_and_continue (来自服务器：novel_mcp)
INFO:OCO_MCPClient:✅ 服务器 novel_mcp 连接成功
```

### 5.3 工具调用测试

```
4. 测试调用 list_nodes...
   结果: []
```

---

## 六、关键技术点

### 6.1 asyncio subprocess 与 MCP 协议

MCP 协议基于 JSON-RPC over stdio：
- Client 通过 stdin 发送请求
- Server 通过 stdout 发送响应
- stderr 用于日志（不被协议使用）

### 6.2 响应匹配机制

```python
# Client 维护一个 future 字典
self._response_futures: Dict[int, asyncio.Future] = {}

# 发送请求时创建 future
async def _send_request(self, method: str, params: Dict[str, Any]) -> Dict[str, Any]:
    req_id = self.request_id
    self._response_futures[req_id] = asyncio.get_event_loop().create_future()
    # 发送请求...
    return await self._response_futures[req_id]  # 等待响应

# 后台任务匹配响应
async def _handle_message(self, msg: Dict[str, Any]):
    msg_id = msg.get("id")
    if msg_id in self._response_futures:
        future = self._response_futures.pop(msg_id)
        if "error" in msg:
            future.set_exception(Exception(msg["error"]))
        else:
            future.set_result(msg.get("result"))
```

### 6.3 为什么 mcp 库 ClientSession 失败

推测原因：
1. mcp 库使用 `MemoryObjectReceiveStream` 作为消息队列
2. Server 的 JSON-RPC 消息通过 `model_validate_json()` 解析
3. Server 发送的响应格式可能与 Client 期望的 Pydantic 模型不完全匹配
4. 导致消息被静默丢弃或无法正确路由

手动实现绕过了这些问题，因为我们直接处理原始 JSON。

---

## 七、遗留问题

### 7.1 RuntimeWarning: coroutine was never awaited

```python
def disconnect_all(self):
    for client in self.servers.values():
        asyncio.create_task(client.disconnect())  # 创建 task 但未等待
```

**状态**：warning，不影响功能
**建议**：在异步上下文中正确清理

### 7.2 list_nodes 返回空列表

```python
result: []  # LangGraph /nodes 端点可能无内容
```

**状态**：协议正常工作，需要检查 LangGraph 端点

---

## 八、总结

### 根因

OCO 系统 MCP 连接故障的**根本原因**是：
1. `bot_server.py` 使用 `except Exception` 而非 `except BaseException`
2. `client.py` 存在严重缩进错误
3. **`mcp` 库的 `ClientSession` 与 Server 实现之间存在协议级兼容性问题**，导致 `initialize()` 调用永久挂起

### 解决方案

创建了完全独立于 `mcp` 库的轻量级实现：
- `simple_client.py`：纯 asyncio subprocess MCP Client
- 重写 `novel_mcp_server.py`：纯 JSON-RPC Server

### 教训

1. **版本依赖要谨慎**：`mcp` 库版本变化可能导致 API 不兼容
2. **第三方框架与协议实现的边界**：使用框架时要注意框架与底层协议的手动实现边界
3. **缩进错误的危害**：Python 缩进错误可能导致代码逻辑完全改变

---

*文档生成时间：2026-04-09*
*排查耗时：约 4 小时*
*修复文件：5 个*

### 第八阶段：LangGraph 异步 SQLite 初始化问题（2026-04-16 更新）

**问题现象**：通过飞书发送测试消息时，LangGraph 节点执行抛出运行时错误：
```
任务执行失败：object str can't be used in 'await' expression
```

**排查过程**：
1. 定位到错误发生在 `Project_Omega_OCO/core/graph.py` 中的 `AsyncSqliteSaver` 初始化。
2. 发现原有代码为 `self._memory = AsyncSqliteSaver(self.checkpoint_db_path)`，其中传入了一个字符串路径。
3. 根据 `langgraph.checkpoint.sqlite.aio` 的要求，`AsyncSqliteSaver` 必须接收一个已经建立的 `aiosqlite.Connection` 对象，而不能直接接收字符串路径。

**修复方案**：
修改 `graph.py`，先使用 `await aiosqlite.connect()` 建立连接，再将连接对象传给 `AsyncSqliteSaver`：
```python
import aiosqlite
self._conn = await aiosqlite.connect(self.checkpoint_db_path)
self._memory = AsyncSqliteSaver(self._conn)
```

### 第九阶段：MCP 客户端上下文管理的致命崩溃（2026-04-16 更新）

**问题现象**：在修复上述问题后，运行 `test_oco_end_to_end.py` 进行端到端测试时，程序在连接第二个 MCP 服务器（`internet_tools`）时没有任何报错直接退出（Exit Code 0）。

**排查过程**：
1. 逐步追踪发现，程序并非正常结束，而是遭遇了未被 `except Exception` 捕获的致命异常，最终导致 `asyncio.run()` 终止。
2. 深入测试发现，使用官方 `mcp` 库的 `stdio_client` 时，如果配合 `contextlib.AsyncExitStack` 来管理多个服务器的上下文生命周期，极易触发 `anyio` 内部的任务组（TaskGroup）状态冲突。
3. 具体的底层错误为：`RuntimeError: Attempted to exit a cancel scope that isn't the current tasks's current cancel scope`。当 `AsyncExitStack` 尝试以 LIFO 顺序关闭由 `anyio` 创建的子进程上下文时，如果事件循环的 TaskGroup 栈不匹配，就会抛出该错误，并迅速演变为 `SystemExit` 或未捕获异常导致父进程直接崩溃。

**修复方案**：
彻底放弃在 `OCO_MCPClient` 中使用 `AsyncExitStack`，改为显式手动调用异步生成器的 `__aenter__` 和 `__aexit__` 方法来管理 `stdio_client` 和 `ClientSession`。
```python
# 修复后的连接逻辑
ctx = stdio_client(server_params)
read, write = await ctx.__aenter__()
session = ClientSession(read, write)
await session.__aenter__()

# 修复后的清理逻辑
await session.__aexit__(None, None, None)
await ctx.__aexit__(None, None, None)
```
此方案完美绕过了 `anyio` 的 TaskGroup 生命周期校验问题，支持了多个 MCP 服务器的稳定并发连接。测试脚本 `test_oco_end_to_end.py` 最终成功跑通了整个 OCO 认知闭环。

### 第十阶段：LangGraph 状态序列化错误（2026-04-16 更新）

**问题现象**：在通过飞书发送消息时，由于飞书后台任务传递了一个用于更新发送进度的 `progress_callback` 函数，系统报出严重错误：
```
任务执行失败：Type is not msgpack serializable: function
```

**排查过程**：
1. 分析错误提示发现，LangGraph 的 Checkpointer (即 `AsyncSqliteSaver`) 在保存认知图状态（State）时，使用了 `msgpack` 序列化。
2. 检查 `Project_Omega_OCO/core/graph.py`，发现 `run` 方法将传入的 `progress_callback` 函数直接赋值给了 `initial_state` 的 `_progress_callback` 字段。
3. 根据 **LangGraph State Serialization Rule** 原则，图的状态对象必须是完全可序列化的字典，包含不可序列化对象（如 Python 函数）会导致系统在尝试向 SQLite 写入检查点时崩溃。

**修复方案**：
引入 Python 内置的异步上下文变量 (`contextvars`)，将 `progress_callback` 从状态流转中彻底剥离，以非侵入的方式安全传递给各个节点。
1. 新建 `Project_Omega_OCO/core/context.py` 定义全局上下文变量：
   ```python
   import contextvars
   progress_callback_var = contextvars.ContextVar('progress_callback', default=None)
   ```
2. 修改 `graph.py` 中的执行入口，使用 `contextvars` 包装运行上下文，移除 `initial_state` 中的回调函数注入：
   ```python
   from .context import progress_callback_var
   token = progress_callback_var.set(progress_callback)
   try:
       final_state = await self.app.ainvoke(initial_state, config=config)
   finally:
       progress_callback_var.reset(token)
   ```
3. 在 `nodes/planner.py` 和 `nodes/executor.py` 等各个处理节点中，安全读取全局上下文变量以执行回调：
   ```python
   try:
       from ..core.context import progress_callback_var
       progress_callback = progress_callback_var.get()
   except Exception:
       progress_callback = None
   ```
此方案彻底消除了不可序列化函数对象对 LangGraph 状态机的影响，成功实现了安全的跨层级进度回调。

### 第十一阶段：Planner 工具幻觉与飞书反馈循环优化（2026-04-16 更新）

**问题现象**：
1. 当用户发送类似 "你好" 或单纯的问候等无需调用工具的消息时，系统进入认知闭环后，`Planner` 节点由于必须返回带 `tool` 字段的 JSON，会幻觉编造出类似 `none`、`analyze_requirements` 等不存在的工具。
2. 这导致 `Executor` 执行失败（`未知工具：none`），触发 `Critic` 评判失败并重新规划，最多重复 10 次后强制终止（`Max iterations reached`）。
3. 飞书的进度通知中只有百分比和阶段，用户在多轮重新规划期间会以为机器人卡死在无限死循环中。

**排查与修复**：
1. **进度回调增加轮次标识**：修改 `cloud_brain/bot_server.py` 中的 `progress_callback`，引入 `plan_version` 计数器，在每次接收到 `计划生成完成` 详情时自增。飞书消息抬头变更为 `🔄 任务处理中 (第 X 轮)...`，显著提升多轮认知迭代时的用户体验。
2. **定义直通回答工具**：修改 `nodes/planner.py` 的 System Prompt，在可用工具列表尾部强行注入伪工具说明：
   ```text
   - direct_response: 如果目标只是普通的聊天、问候或可以直接回答的问题，请使用此工具。参数包含 'response' 字段存放你的直接回答。
   ```
3. **Executor 绕过 MCP 拦截直通**：修改 `nodes/executor.py`，在实际调用 MCP 之前拦截 `direct_response` 工具：
   ```python
   if tool_name == "direct_response":
       observation = params.get("response", "无法生成直接响应。")
   else:
       observation = await self.mcp_client.call_tool(tool_name, params)
   ```
此方案完美解决了 OCO 认知闭环在面临“无工具可用”时的异常震荡问题，并提高了系统面对普通闲聊和闲散指令的健壮性。

### 第十二阶段：长文本撑爆 Critic 解析导致死循环（2026-04-16 更新）

**问题现象**：
1. 用户下发了长篇小说写作任务，Planner 因为有了 `direct_response` 工具，决定“越俎代庖”，直接将数千字的小说正文写入到了 `direct_response` 的 `response` 参数中。
2. Executor 将这几千字的原样输出作为 `SubTaskResult`，传递给 `Critic` 节点进行质量审计。
3. `Critic` 在进行 LLM 评分时，由于 Prompt 中的 `eval_context` 包含了这几千字的完整小说内容，导致 LLM 生成的 JSON 评估结果过长或被意外截断。
4. JSON 解析失败（报错 `JSON parse error: Expecting property name enclosed in double quotes`），导致 `is_passed` 默认为 False，任务被迫进入无意义的重复循环（甚至达到了 6 轮以上）。
5. 虽然在第 6 轮偶尔成功解析，但前置无意义的重试加上本身极大的 Token 量，最终触发了 300 秒的全局系统超时（`[OCO Adapter] 执行超时（300 秒）`）。

**排查与修复**：
1. **防爆截断处理**：在 `nodes/critic.py` 中增加对 `results` 的长度限制，保留头尾关键信息（前 1000 字符 + 后 1000 字符），防止超长执行结果污染 LLM 审计上下文。
   ```python
   def truncate_result(res):
       try:
           res_str = str(res)
           if len(res_str) > 2000:
               return res_str[:1000] + "\n...[内容过长已截断]...\n" + res_str[-1000:]
           return res_str
       except:
           return str(res)
   ```
2. **扩大全局超时限制**：将 `adapter.py` 中的同步调用 `timeout` 从 300 秒放宽至 600 秒，为大型创作任务提供充分的生成窗口。

此举保证了无论单次工具调用返回的内容多么庞大，`Critic` 都能始终稳定输出结构化的 JSON 评判结果，避免陷入因为解析异常导致的无限重试。

### 第十三阶段：捍卫“流程正义”，防止模型越俎代庖（2026-04-16 更新）

**问题现象**：
在解决长文本超时问题时，最初的方案是截断 `Critic` 的输入文本以防解析报错。但这违背了**“流程正义”**，因为这掩盖了真正的问题：`Planner`（本地模型 Qwen 3.5 27B）并未履行“拆解任务并调度工具”的职责，而是错误地利用 `direct_response` 虚拟工具，强行一口气输出了上千字的小说正文。这不仅会导致飞书手机端阅读体验变差（如果是截断的半成品内容），更重要的是破坏了流水线（Pipeline）设计的核心意义，导致后续针对 AI 视频生成、游戏开发等需要多步骤严格协同的流水线无法正常运作。

**排查与修复**：
1. **撤销截断，保留真实反馈**：回滚 `nodes/critic.py` 中的截断逻辑，确保执行结果完整无损地流转和透传。
2. **重塑 Planner 角色定位（强制流程正义）**：大幅强化了 `nodes/planner.py` 中的 System Prompt。明确定义其身份为**“任务规划与调度器”**，加入【流水线与工具使用规范】：
   - **严禁越俎代庖**：明确禁止 Planner 在不调用专业工具的情况下直接生成长篇内容或交付物。
   - **强制任务拆解**：要求对于复杂任务（如小说创作），必须拆解为多个步骤并调用对应工具（如 `generate_outline`, `write_chapter`）。
   - **严格限制 `direct_response`**：明确警告该虚拟工具仅能用于“简单闲聊”和“能力缺失反馈”，绝对不能用于输出正文或代码。

此架构级调整彻底纠正了 Planner 的认知越界问题，确保了系统按预期的多节点流水线（先大纲、后正文）稳定运行，为未来的复杂工作流奠定了坚实的基础。

### 第十四阶段：回归架构初心，废除提示词补丁，恢复硬路由分流（2026-04-16 更新）

**问题现象**：
在回顾了 `OMEGA_COGNITIVE_ARCHITECTURE.md` 等架构文档后，我们深刻意识到，此前引入 `direct_response` 工具，并通过繁杂的 Prompt 试图约束 Planner 不要在闲聊时“越俎代庖”生成大段内容，完全是走偏了。
这是一种典型的**“用提示词硬解架构问题（Phase 2）”**的错误思路：
1. 最初为了追求“大一统”，我们将 OCO Router 中的 `assess_complexity` 评估强行绕过，让**所有请求（包括“你好”）**都进入了 OCO 认知闭环。
2. 导致 Planner 在遇到“你好”时无可用工具，陷入死循环报错。
3. 为掩盖这个报错，我们引入了 `direct_response` 虚拟工具，却引发了 Planner “抄近道”生成几千字小说的灾难。

**排查与修复**：
1. **重构 OCO Router，恢复架构级分流**：
   修改 `router.py`，重新启用任务复杂度评估机制。现在，Router 会基于规则和上下文判断：
   - 简单问候或闲聊（长度短且匹配 `simple_keywords`） -> 返回 `RoutePath.LEGACY`。
   - 复杂请求（匹配特定关键字或长度较长） -> 返回 `RoutePath.OCO`。
2. **清理 `bot_server.py` 的执行通道**：
   确保 `bot_server.py` 严格遵守 Router 的决策。如果判定为 `LEGACY`，不再启动耗时的状态机，而是直接调用底层的 LLM 快速响应（极速模式）。
3. **净化 Planner 与规范“能力缺失”**：
   - 彻底删除 Planner 提示词中的 `direct_response` 补丁，使得 Planner 永远无法处理闲聊，只能专注调度 MCP 真实工具。
   - 增加规范的“能力缺失报告”机制：如果 Planner 发现任务需要未接入的能力，必须调度内置的 `CAPABILITY_MISSING` 动作，并在 `params` 中说明原因。
4. **Executor 拦截能力缺失**：
   在 `Executor` 节点硬编码拦截 `CAPABILITY_MISSING`，当捕获到该指令时，不再发起真实的 MCP 请求，而是组装一条优雅的提示语返回给用户（如：“抱歉，该任务需要用到【视频生成】工具，但目前系统尚未接入此能力，请您先尝试构建。”），然后正常结束闭环。

这次重构拔掉了所有治标不治本的 Prompt 补丁，让架构真正回到了最初设计的 **“各司其职、硬路由分流、代码级状态机兜底”** 的 Phase 3 阶段，彻底根除了闲聊进入重认知闭环导致的各种光怪陆离的 Bug。
